# Default values for readwillbe.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/jwhumphries/readwillbe
  pullPolicy: IfNotPresent
  # Published tags carry a "v" prefix, so this cannot be left empty to fall
  # back to .Chart.AppVersion ("0.1.1") -- that tag does not exist. Keep it
  # pinned explicitly and bump it alongside appVersion.
  tag: "v0.1.1"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  fsGroup: 10001 # Matches the nonroot user in Dockerfile

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false # SQLite needs write access to db directory, or we mount specific volume
  runAsNonRoot: true
  runAsUser: 10001

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

persistence:
  enabled: true
  storageClass: "" # Host default
  accessMode: ReadWriteOnce
  size: 1Gi
  path: /data

# Application Configuration
env:
  READWILLBE_PORT: ":8080"
  READWILLBE_LOG_LEVEL: "info"
  # READWILLBE_DB_PATH will be set automatically based on persistence.path if enabled
  READWILLBE_ALLOW_SIGNUP: "true"
  READWILLBE_SEED_DB: "false"
  TZ: "America/New_York"
  READWILLBE_HOSTNAME: "http://localhost:8080"

# Secrets - You should provide these via --set or a separate values file in production
secrets:
  # Name of an existing Secret to read all credentials from. When set, this
  # chart renders no Secret of its own and the keys below are ignored.
  # Expected keys: cookie-secret, and optionally vapid-public-key,
  # vapid-private-key, smtp-password, resend-api-key.
  # Prefer this under GitOps: a chart-generated secret is regenerated on every
  # render, which shows as permanent drift and invalidates sessions.
  existingSecret: ""
  # Generates a random one if empty (helm function) or you must provide it
  cookieSecret: ""
  vapidPublicKey: ""
  vapidPrivateKey: ""
  # Email secrets (only one provider should be configured)
  smtpPassword: ""
  resendApiKey: ""

# Email Configuration
# Set email.provider to "smtp" or "resend" to enable email notifications
# Only one provider can be active at a time
email:
  # Provider: "smtp", "resend", or "" (disabled)
  provider: ""

  # SMTP Configuration (used when provider: "smtp")
  smtp:
    host: ""
    port: 587
    username: ""
    # Password is stored in secrets.smtpPassword
    from: ""  # e.g., "ReadWillBe <noreply@example.com>"
    tls: "starttls"  # "none", "starttls", or "tls"

  # Resend Configuration (used when provider: "resend")
  resend:
    # API key is stored in secrets.resendApiKey
    from: ""  # e.g., "ReadWillBe <noreply@yourdomain.com>"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
